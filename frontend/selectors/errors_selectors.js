

export const answerErrors = (state) => state.errors.answer;

export const sessionErrors = (state) => state.errors.session;

export const questionErrors = (state) => state.errors.question;

export const commentErrors = (state) => state.errors.comment;